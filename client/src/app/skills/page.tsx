import { SkillsListView } from "./_components/SkillsListView";

/* Route: /skills (Skills Lab list). Thin route entry — the view, its import
   drawer, styles, constants, and helpers are colocated under _components/SkillsListView. */
export default function SkillsPage() {
  return <SkillsListView />;
}
